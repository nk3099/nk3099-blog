---
import { getCollection } from "astro:content";
import Layout from "@/layouts/Layout.astro";
import Header from "@/components/Header.astro";
import Footer from "@/components/Footer.astro";
import Socials from "@/components/Socials.astro";
import LinkButton from "@/components/LinkButton.astro";
import Card from "@/components/Card.astro";
import Hr from "@/components/Hr.astro";
import getSortedPosts from "@/utils/getSortedPosts";
import IconRss from "@/assets/icons/IconRss.svg";
import IconArrowRight from "@/assets/icons/IconArrowRight.svg";
import { SITE } from "@/config";
import { SOCIALS } from "@/constants";

const posts = await getCollection("blog");

const sortedPosts = getSortedPosts(posts);
const featuredPosts = sortedPosts.filter(({ data }) => data.featured);
const recentPosts = sortedPosts.filter(({ data }) => !data.featured);

---

<Layout>
  <Header />
  <main id="main-content" data-layout="index">
    <section id="hero" class="pt-8 pb-6">

     <!-- Image aligned to the right -->
      <img src="/neeraj.jpeg"
        alt="Neeraj Kumar"
        class="float-right ml-6 mb-4 w-28 h-28 sm:w-36 sm:h-36 md:w-40 md:h-40 rounded-full object-cover shadow-lg ring-1 ring-gray-200"
        loading="eager"
        decoding="async"
      />


      <h1 class="my-4 inline-block text-3xl font-bold sm:my-8 sm:text-4xl">
       Hey, 👋  I’m Neeraj.
   </h1>
      <!--
      
      <a
        target="_blank" 
        href="/rss.xml"
        class="inline-block"
        aria-label="rss feed"
        title="RSS Feed">
     <IconRss
          width={20}
          height={20}
          class="scale-125 stroke-accent stroke-3"
        />
        <!-- <span class="sr-only">RSS Feed</span> -->
      </a>


      <p> My journey in technology began with curiosity and evolved into a passion for creating exceptional customer experiences and building solutions that deliver measurable impact.
     <div class="mt-6">Today, I work at <LinkButton
          class="underline decoration-dashed underline-offset-4 hover:text-accent"
          href="https://Sprinklr.com"> Sprinklr </LinkButton>,
      as a Platinum Account Manager, 
 leading strategic customer engagement for enterprise clients while drawing on my experience managing critical incidents within Cloud Engineering - ensuring reliability and resilience at scale through proactive monitoring and rapid response strategies.
       </div>

  <div class="mt-6"> Outside of work, you’ll likely find me exploring new ideas, learning emerging technologies, and sharing insights that help businesses thrive.
  </div>


      <!--</p>
      <p class="mt-2">
        Read the blog posts or check 
        <LinkButton
          class="underline decoration-dashed underline-offset-4 hover:text-accent"
          href="/about"
        >
        my bio
        </LinkButton> for more info.
      </p> -->

      <br>
      <br>
      <br>
      <br>
      
      {
        // only display if at least one social link is enabled
        SOCIALS.length > 0 && (
          <div class="mt-4 flex flex-col sm:flex-row sm:items-center sm:justify-center text-center">
            <div class="mr-2 mb-1 whitespace-nowrap sm:mb-0"></div>
            <Socials />
          </div>
        )
      }
    </section>

<!--
    <Hr />

    {
      featuredPosts.length > 0 && (
        <>
          <section id="featured" class="pt-12 pb-6">
            <h2 class="text-2xl font-semibold tracking-wide">Featured</h2>
            <ul>
              {featuredPosts.map(data => (
                <Card variant="h3" {...data} />
              ))}
            </ul>
          </section>
          {recentPosts.length > 0 && <Hr />}
        </>
      )
    }

    {
      recentPosts.length > 0 && (
        <section id="recent-posts" class="pt-12 pb-6">
          <h2 class="text-2xl font-semibold tracking-wide">Recent Posts</h2>
          <ul>
            {recentPosts.map(
              (data, index) =>
                index < SITE.postPerIndex && <Card variant="h3" {...data} />
            )}
          </ul>
        </section>
      )
    }


    <div class="my-8 text-center">
      <LinkButton href="/posts/">
        All Posts
        <IconArrowRight class="inline-block" />
      </LinkButton>
    </div>
  </main>
  <Footer />
</Layout>

>
<script>
  document.addEventListener("astro:page-load", () => {
    const indexLayout = (document.querySelector("#main-content") as HTMLElement)
      ?.dataset?.layout;
    if (indexLayout) {
      sessionStorage.setItem("backUrl", "/");
    }
  });
</script>

-->
